<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccj.campus.chat.mapper.CourseClassRelMapper">

    <insert id="insertRel">
        INSERT INTO course_class_rel (course_id, class_id)
        VALUES (#{courseId}, #{classId}) ON CONFLICT (course_id, class_id) DO NOTHING
    </insert>

    <delete id="deleteRel">
        DELETE
        FROM course_class_rel
        WHERE course_id = #{courseId}
          AND class_id = #{classId}
    </delete>

    <select id="listClassIdsByCourse" resultType="java.lang.Long">
        SELECT class_id
        FROM course_class_rel
        WHERE course_id = #{courseId}
    </select>

</mapper>