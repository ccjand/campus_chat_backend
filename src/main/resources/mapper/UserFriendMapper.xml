<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccj.campus.chat.mapper.UserFriendMapper">

    <resultMap id="BaseResultMap" type="com.ccj.campus.chat.entity.UserFriend">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="friend_id" property="friendId"/>
        <result column="remark" property="remark"/>
        <result column="deleted" property="deleted"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- 好友列表（只查未删除的） -->
    <select id="listByUser" resultMap="BaseResultMap">
        SELECT *
        FROM user_friend
        WHERE user_id = #{userId}
          AND deleted = FALSE
        ORDER BY create_time DESC
    </select>

    <!-- 查询两人之间是否存在好友关系（任一方向） -->
    <select id="selectRelation" resultMap="BaseResultMap">
        SELECT *
        FROM user_friend
        WHERE ((user_id = #{userId} AND friend_id = #{friendId})
            OR (user_id = #{friendId} AND friend_id = #{userId}))
          AND deleted = FALSE LIMIT 1
    </select>

</mapper>